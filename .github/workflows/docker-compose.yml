name: Test Docker Compose - Geneweb (preprod → main)

on:
  pull_request:
    branches:
      - main
    types: [opened, synchronize, reopened]

jobs:
  build-and-test:
    if: github.base_ref == 'main' && github.head_ref == 'preprod'
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ./geneweb-python/docker

    steps:
      - name: 🧩 Checkout repository
        uses: actions/checkout@v4

      - name: 🐋 Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: 🔧 Build and start services
        run: |
          docker compose up -d --build
          echo "Containers launched. Waiting for startup..."
          sleep 15
          docker compose ps

      - name: ✅ Check running containers
        run: |
          docker ps
          docker ps | grep gwd
          docker ps | grep gwsetup

      - name: 🌐 Test endpoints (with retry)
        run: |
          echo "Waiting for gwd service..."
          for i in {1..20}; do
            if curl -fs http://localhost:2317 > /dev/null; then
              echo "✅ gwd is up!"
              break
            fi
            echo "⏳ Waiting for gwd... ($i)"
            sleep 3
          done

          echo "Waiting for gwsetup service..."
          for i in {1..20}; do
            if curl -fs http://localhost:2316 > /dev/null; then
              echo "✅ gwsetup is up!"
              break
            fi
            echo "⏳ Waiting for gwsetup... ($i)"
            sleep 3
          done

      - name: 🧹 Clean up
        if: always()
        run: docker compose down
 